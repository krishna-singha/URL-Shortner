<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, Helvetica, sans-serif;
        }
        body {
            width: 100vw;
            background-color: #f1f1f1de;
        }
        #container {
            width: 100%;
            height: 100%;
            padding: 2rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 4rem;
        }
        h1 {
            font-size: 3rem;
        }
        #form {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        form {
            background-color: rgba(128, 128, 128, 0.458);
            padding: 5rem 2rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 1rem;
            width: 30%;
            border-radius: 0.5rem;
        }
        label, input, button {
            text-align: center;
            font-size: 1.5rem;
            width: 100%;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 0.5rem;
        }
        label:focus, 
        input:focus,
        button:focus {
            outline: none;
        }
        button {
            background-color: #4CAF50;
            color: white;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            border: 1px solid black;
        }
        th {
            background-color: rgba(0, 0, 0, 0.737);
            color: white;
        }
        th, td {
            min-width:15rem;
            height: 3rem;
            text-align: center;
        }

    </style>
</head>

<body>
    <div id="container">
        <h1>URL Shortener</h1>
    <% if (locals.Id) { %>
        <p>URL Generated: https://url-shortner-blond-seven.vercel.app/url<%= Id %> - <a href="https://url-shortner-blond-seven.vercel.app/url/<%= Id %>">CLick Here</a></p>
        <% } %>
            <div id="form">
                <form method="post" action="/url">
                    <label for="url">Enter Original URL</label>
                    <input type="text" name="url" placeholder="https://example.com">
                    <button type="submit">Generate</button>
                </form>
            </div>
            <% if (locals.urls) { %>
                <table>
                    <thead>
                        <th>S.No</th>
                        <th>ShortId</th>
                        <th>Redirect</th>
                        <th>Clicks</th>
                    </thead>
                    <tbody>
                        <% urls.forEach((url, index)=> { %>
                            <tr>
                                <td>
                                    <%= index + 1 %>
                                </td>
                                <td>
                                    <%= url.shortId %>
                                </td>
                                <td>
                                    <%= url.redirectUrl%>
                                </td>
                                <td>
                                    <%= url.visitHistory.length %>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
                <% } %>
    </div>

</body>

</html>